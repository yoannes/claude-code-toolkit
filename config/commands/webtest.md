---
description: Test new features using Chrome browser automation
---

Using the webapp test skill (chrome) - test the features you just made logically if they are not yet validated.

If login is required, credentials are stored in the project's .env file. Read .env to find the relevant username/email and password variables.
